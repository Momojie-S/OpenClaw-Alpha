[project]
name = "openclaw-alpha"
version = "0.1.0"
description = "OpenClaw stock & finance analysis skill"
requires-python = ">=3.10"
dependencies = [
    "tushare>=1.4.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
]

[tool.uv]
dev-dependencies = []
